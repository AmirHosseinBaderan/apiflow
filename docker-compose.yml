services:
  api-flow:
    build: .
    ports:
      - "8080:80"
      - "3001:3001"
    environment:
      - PORT=3001
      - DATA_DIR=/data
      - JWT_SECRET=change-me-in-production
    volumes:
      - ./data:/data
    restart: unless-stopped
